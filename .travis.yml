language: node_js
env:
  - YARN_GPG=no
os:
  - linux
  - osx
node_js:
  - "node"
branches:
  only:
    - master
cache:
  directories:
  - node_modules/
script:  cd packages/dscc-gen && npm install && npm run travis
addons:
  apt:
    packages:
      - expect
